# Contact Form Webhook
# Replace with your webhook URL
PUBLIC_CONTACT_WEBHOOK_URL=
PUBLIC_TRAINING_WAITLIST_WEBHOOK_URL=

# Passcode gating /audits/<slug>/ for each client — see src/data/audit-clients.ts.
# Add one of these per client, named to match that client's envVar entry.
AUDIT_PASSWORD_ABSOLUTE_INTEGRATED_HEALTH=